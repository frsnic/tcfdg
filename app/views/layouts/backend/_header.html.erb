<nav class="navbar navbar-fixed-top navbar-dark bg-inverse">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">
      <span class="glyphicon glyphicon-home" style="padding-right: 10px;"></span>
      <%= Setting.name %>
    </a>
    <% if current_user %>
      <ul class="nav navbar-nav pull-right">
        <li class="nav-item">
          <%= link_to "哈囉，#{current_user.name}", "javascript: void(0)", class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete">
            Logout
            <span class="glyphicon glyphicon-log-out" style="padding-left: 10px;"></span>
          </a>
        </li>
      </ul>
    <% end %>
  </div>
</nav>

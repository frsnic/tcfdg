<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a href="/" class="navbar-brand" data-no-turbolink="true" >
      <div style="font-size: 36px">台中城市發展田調團</div>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse flex-column-reverse align-items-end" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "關於田調團", about_us_path, class: 'nav-link', data: { no_turbolink: true } %>
        </li>
        <% Category.where(id: [10, 17, 38]).select(:id, :name, :handle).find_each do |category| %>
          <li class="nav-item">
            <%= link_to category.name, "/#{category.handle}", class: 'nav-link', data: { no_turbolink: true } %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to "活動資訊", "/activities", class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "相關新聞", frontend_news_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to "聯絡我們", contact_us_path, class: 'nav-link', data: { no_turbolink: true } %>
        </li>
      </ul>
      <%= form_tag search_path, method: :get, class: "d-flex", role: "search" do |f| %>
        <div class="input-group">
          <%= text_field_tag :q, "", type: :search, placeholder: "Search", class: "form-control me-2", 'aria-label': "Search" %>
          <div class="input-group-btn">
            <button class="btn btn-default" type="submit"><i class="fas fa-search"></i></button>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</nav>

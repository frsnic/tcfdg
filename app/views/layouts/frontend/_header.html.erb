<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand">
        <%= image_tag "/images/logo_text.gif", class: "img" %>
      </a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <div class="pull-right">
        <%= form_tag search_path, method: :get, class: "navbar-form", role: "search" do |f| %>
          <div class="input-group">
            <%= text_field_tag :q, "", type: :search, placeholder: "Search", class: "form-control" %>
            <div class="input-group-btn">
              <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
          </div>
        <% end %>
      </div>

      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "關於田調團", about_us_path, data: { no_turbolink: true } %>
        </li>
        <% Category.where(id: [10, 17, 38]).select(:id, :name, :handle).find_each do |category| %>
          <li>
            <%= link_to category.name, "/#{category.handle}", data: { no_turbolink: true } %>
          </li>
        <% end %>
        <li>
          <%= link_to "活動資訊", "/activities" %>
        </li>
        <li>
          <%= link_to "相關新聞", frontend_news_path %>
        </li>
        <li>
          <%= link_to "聯絡我們", contact_us_path, data: { no_turbolink: true } %>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>
